---
language: c
env:
  matrix:
        - APP_PATH="Timer using Pure C"
before_install:
  - sudo apt-get update -qq
install:
  - sudo apt-get install -qq gcc-avr binutils-avr avr-libc
script:
  - avr-gcc --version
  - cd $APP_PATH
  - make
